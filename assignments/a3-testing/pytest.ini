[pytest]
# Anchors pytest rootdir here so every student's phase1/tests pytest run
# picks up the shared conftest.py at this level (which handles the module
# import path for the compiled .pyc bundle in starter/assignment3/modules/).
